<%- include('partials/page-header') %>

<div class="row">
  <div class="col-lg-8">
    <div class="content-section">
      <%- content.body %>
    </div>
  </div>
  <div class="col-lg-4">
    <div class="card shadow-sm mb-4">
      <div class="card-header bg-primary text-white">
        <i class="bi bi-info-circle"></i> Quick Info
      </div>
      <div class="card-body">
        <p>This is a sidebar that could contain related links, quick information, or a call to action.</p>
        <% if (typeof content.sidebarContent !== 'undefined') { %>
          <%- content.sidebarContent %>
        <% } %>
      </div>
    </div>
  </div>
</div>

<%- include('partials/page-footer') %>